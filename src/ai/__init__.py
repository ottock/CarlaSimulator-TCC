"""AI workstream (behavioral cloning) for the CARLA digital twin.

Runs on the training PC (Python 3.12). The ``ai.shared`` subpackage is written
in Python 3.6-compatible syntax so the exact same preprocessing can later be
reused on the Jetson Nano (future hardware plan).
"""
